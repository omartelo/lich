.task
bin
# Stray root binaries from `go build .` (use `task build` -> bin/)
/lich
# IDE
.idea/
frontend/dist
frontend/node_modules
frontend/coverage
# tsc --build incremental cache
*.tsbuildinfo
# Claude Code local state (skills restore from skills-lock.json)
.claude/
